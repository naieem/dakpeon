(function(w) {
    angular
        .module('login')
        .controller('loginController', constructor);

    /* @ngInject */
    function constructor() {

    }

    constructor.$inject = [];
})(window);